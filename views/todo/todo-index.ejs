<% include ../partials/boilerplate %>
<div class="navbar">
    <img class="navlogo" src="./images/mainLogo.png" alt="">
    <a class="navbuttons" href='/'>Home</a>
    <a class="navbuttons" href='/auth/register'>Register</a>
    <a class="navbuttons" href='/user'>Profile</a>
    <a class="navbuttons" href='/auth/login'>Login</a>
    <a class="navbuttons" href='/auth/logout'>Logout</a>
</div>
<div class="indexContainer">
    <a class="newtask" href="/todo/add">Add a new task</a>
<% for (let todo of data) { %>
    <div class="taskContainer">
        <div class="one">
            <h1 class="first"><%= todo.title %></h1> 
            <h3 class="first"><%= todo.status %></h3>
        </div>
        <div class="two">
            <h2 class=""><%= todo.category %></h2>
            <a class="second" href='/todo/<%= todo.id%>'>Info</a>
            <a class="second" href="/todo/<%=todo.id%>/edit">Edit</a>
            <form class="second" method="POST" action="/todo/<%= todo.id %>?_method=DELETE">
                <input type='submit' value='Delete' />
            </form>   
        </div>    
    </div>
<% } %>
</div>

<% include ../partials/end %>